extends /layouts/application.pug

block content
  +formFor(f, { url: `/users/${userId}`, method: 'patch' })
    +input(f, 'email')
    +input(f, 'firstName')
    +input(f, 'lastName')
    //- +input(f, 'password', { as: 'password' })
    +button('Edit')    
